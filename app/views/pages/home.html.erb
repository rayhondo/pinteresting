<div class = "jumbotron center">
	<h1>Sup</h1>

	<% if user_signed_in? %>
		# do something
	<% else %>
		<p>Click here to <%= link_to "Sup", new_user_registration_path, class: "btn btn-default btn-lg"%>.</p>
		<%= link_to "Login", new_user_session_path, class: "btn btn-primary btn-lg"%>
	<% end %>
</div>